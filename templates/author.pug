
mixin author(authorId)
  - var author = Object.values(contents.authors).find(author => author.metadata.id === authorId);
  span.author
    if author
      a(href='mailto:' + author.metadata.email)= author.metadata.name
    else
      = "unknown"
